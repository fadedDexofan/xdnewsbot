const commands = require("./commands");
const messages = require("./messages");
const actions = require("./actions");
const payments = require("./payments");

module.exports = {
  commands, messages, actions, payments,
};
